-----------------------------------ИСКЛЮЧЕНИЯ И StackTrace-------------------------------------------
task0902 - возвращает имя метода, вызвавшего его, полученное с помощью StackTrace
task0903-вернуть номер строки кода, из которого вызвали этот метод. функцией: element.getLineNumber().
task0913-throw new IllegalArgumentException("Какоето сообщение");
task1604 - выводит свой стек-трейс в потоке.
task1633 - Почитать про UncaughtExceptionHandler - это важно.
------------------------------------РАБОТА С ФАЙЛАМИ--------------------------------------------------
task0929; - РАБОТА С ФАЙЛАМИ
задача: Программа вводит два имени файла. И копирует первый файл на место, заданное вторым именем.
Если файла (который нужно копировать) с указанным именем не существует, то программа должна вывести надпись
"Файл не существует." и еще один раз прочитать имя файла с консоли, а уже потом считывать файл для записи.
task1318 - Чтение и вывод русских букв из файла
task1319 - Писатель в файл с консоли (запись строки в файл, каждую строчку с новой строки)
task1326 - Считывание чисел из файла, вывод на экран через list.forEach(System.out::println);
task1525 - Читаем текстовый файл построчно
task1721 - allLines = Files.readAllLines(Paths.get(reader.readLine()));//Считать все строки из файла allLines-массив
-------------------------------------------------------------------------------------------------------------
task.task09.task0924;
    public static abstract class StoryItem {
        public ArrayList<StoryItem> killed = new ArrayList<>();
        public ArrayList<StoryItem> ate = new ArrayList<>();
    }
-------------------------------------------------------------------------------------------------------------
task0930 - isNumber - Определяет строка s число или нет
-------------------------------------------------------------------------------------------------------------
task1016 - MAP Collection Map разные решения при работе с MAP
-------------------------------------------------------------------------------------------------------------
task1012 - Маленький пример работы со множеством пример
String abc = "абвгдеёжзийклмнопрстуфхцчшщъыьэюя"; char[] abcArray = abc.toCharArray(); //распарсил в массив чаров
-------------------------------------------------------------------------------------------------------------
task1320 - Заставила думать)))
---------------------------------------регуляры------------------------------------------------------------
s.matches("[+-]?\\d*(\\.\\d+)?")-стр. s  число или нет
---------------------------------------ООП-------------------------------------------------------------------
task1526 - если поменять на protected какая то магия
